import { DialogHeader, DialogTitle } from "@/components/ui/dialog";

export function FAT16Settings() {
  return (
    <>
      <DialogHeader>
        <DialogTitle>File Allocation Table (FAT16) Settings</DialogTitle>
      </DialogHeader>
    </>
  );
}
